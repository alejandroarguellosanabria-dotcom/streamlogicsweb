<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Streamlogics — Clip Agency for Streamers</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,700;0,900;1,400;1,700&family=Outfit:wght@300;400;500;600&display=swap" rel="stylesheet">
<style>
:root {
  --bg:     #e8f4f2;
  --white:  #ffffff;
  --p1:     #c2deda;
  --p2:     #9ecdc8;
  --p3:     #6ab0a8;
  --p4:     #d8eeed;
  --dark:   #0f2e2b;
  --mid:    #1e5c56;
  --muted:  #5a8c87;
  --border: #b0d4cf;
}

*, *::before, *::after { margin:0; padding:0; box-sizing:border-box; }
html { scroll-behavior:smooth; -webkit-text-size-adjust:100%; }
body {
  background:var(--bg);
  color:var(--dark);
  font-family:'Outfit', -apple-system, BlinkMacSystemFont, sans-serif;
  font-weight:300;
  overflow-x:hidden;
  line-height:1.5;
}

/* ═══ NAV ═══ */
nav {
  position:fixed; top:0; left:0; right:0; z-index:100;
  background:var(--white);
  border-bottom:1px solid var(--border);
  height:64px;
  display:flex; align-items:center;
  justify-content:space-between;
  padding:0 5%;
}
.logo {
  font-family:'Playfair Display', Georgia, serif;
  font-size:1.25rem; font-weight:400; color:var(--dark);
  letter-spacing:0.5px;
}
.nav-links {
  display:flex; align-items:center; gap:0;
}
.nav-links a {
  padding:0 20px; height:64px;
  display:flex; align-items:center;
  color:var(--muted); text-decoration:none;
  font-size:0.88rem; font-weight:400;
  border-left:1px solid var(--border);
  transition:all 0.2s;
  white-space:nowrap;
}
.nav-links a:hover { background:var(--p4); color:var(--dark); }
.nav-links .nav-cta {
  background:var(--dark); color:var(--white) !important;
  font-weight:600;
}
.nav-links .nav-cta:hover { background:var(--mid) !important; }

/* ═══ HERO ═══ */
.hero {
  min-height:100svh;
  padding:100px 5% 60px;
  background:var(--bg);
  display:flex; flex-direction:column;
  align-items:center; justify-content:center;
  text-align:center;
}

.hero-badge {
  display:inline-flex; align-items:center; gap:8px;
  background:rgba(255,255,255,0.8);
  border:1px solid var(--border);
  padding:8px 18px; margin-bottom:36px;
  font-size:0.72rem; font-weight:600;
  letter-spacing:2.5px; text-transform:uppercase;
  color:var(--mid);
}

h1 {
  font-family:'Playfair Display', Georgia, serif;
  font-size:clamp(2.8rem, 8vw, 7rem);
  font-weight:700; line-height:1.08;
  color:var(--dark); margin-bottom:24px;
  max-width:800px; letter-spacing:-0.5px;
}
h1 em { font-style:italic; color:var(--p3); }

.hero-sub {
  font-size:clamp(1rem, 2.5vw, 1.35rem);
  color:var(--muted); line-height:1.85;
  max-width:520px; margin:0 auto 40px;
}

.hero-btn {
  display:inline-block;
  background:var(--dark); color:var(--white);
  padding:clamp(16px,3vw,22px) clamp(36px,6vw,64px);
  font-size:clamp(1rem,2.5vw,1.3rem);
  font-weight:600; text-decoration:none;
  font-family:'Outfit',sans-serif;
  transition:background 0.25s;
  margin-bottom:32px;
}
.hero-btn:hover { background:var(--mid); }

/* ═══ PORTFOLIO ═══ */
.portfolio-bg { background:var(--bg); }

.port-grid {
  display:grid; grid-template-columns:repeat(3,1fr);
  gap:clamp(16px,2.2vw,24px);
  margin-top: 40px;
}

.clip {
  background:var(--white);
  border:1px solid var(--border);
  display:flex; flex-direction:column;
  transition:transform 0.4s ease, box-shadow 0.4s ease;
  cursor:pointer;
}
.clip:hover {
  transform:translateY(-4px);
  box-shadow:0 18px 44px rgba(15,46,43,0.10);
}

.clip-frame {
  position:relative;
  aspect-ratio:9/16;
  overflow:hidden;
  background:var(--dark);
}
.clip-img {
  width:100%; height:100%;
  object-fit:cover;
  display:block;
}

.clip-info {
  padding:16px 18px;
  border-top:1px solid var(--border);
  display:flex; justify-content:space-between;
  align-items:flex-end; gap:12px;
}
.clip-title {
  font-family:'Playfair Display',Georgia,serif;
  font-size:clamp(0.95rem,1.6vw,1.1rem);
  font-weight:600; color:var(--dark);
  margin-bottom:3px; line-height:1.25;
}
.clip-streamer {
  font-size:0.78rem; color:var(--muted);
}

/* ═══ PRICING ═══ */
.pricing-bg { background:var(--dark); padding: 80px 5%; }
.pricing-bg h2 { color: var(--white); font-family: 'Playfair Display', serif; font-size: 3rem; margin-bottom: 40px; text-align: center; }

.pricing-grid {
  display:grid; grid-template-columns:repeat(3,1fr);
  gap: 20px;
}
.card {
  padding: 40px;
  background:rgba(255,255,255,0.04);
  border: 1px solid rgba(255,255,255,0.1);
  color: var(--white);
  text-align: center;
}
.card.featured { background: var(--white); color: var(--dark); border: none; transform: scale(1.05); }
.card h3 { font-size: 1.5rem; margin-bottom: 10px; }
.card .price { font-size: 3.5rem; font-family: 'Playfair Display', serif; font-weight: 900; margin-bottom: 20px; }
.card .price span { font-size: 1rem; opacity: 0.6; }

.c-btn {
  display:block; text-align:center;
  padding: 15px; margin-top: 30px;
  background: var(--p2); color: var(--dark);
  text-decoration:none; font-weight:600;
}
.card.featured .c-btn { background: var(--dark); color: var(--white); }

/* ═══ REVEAL ═══ */
.reveal { opacity:0; transform:translateY(20px); transition:opacity 0.7s ease, transform 0.7s ease; }
.reveal.visible { opacity:1; transform:none; }

@media(max-width:900px) {
  .port-grid, .pricing-grid { grid-template-columns: 1fr; }
  .card.featured { transform: none; }
}
</style>
</head>
<body>

<nav>
  <div class="logo">Streamlogics</div>
  <div class="nav-links">
    <a href="#portfolio">Portfolio</a>
    <a href="#pricing">Pricing</a>
    <a href="#pricing" class="nav-cta">Get started →</a>
  </div>
</nav>

<section class="hero">
  <div class="hero-badge">Clip Agency for Streamers</div>
  <h1>Your stream deserves<br>to grow <em>beyond limits.</em></h1>
  <p class="hero-sub">We turn your best moments into viral clips. You stream — we handle the rest.</p>
  <a href="#pricing" class="hero-btn">See packages →</a>
</section>

<section class="section portfolio-bg" id="portfolio" style="padding: 80px 5%;">
  <h2 style="font-family:'Playfair Display'; font-size: 3rem; text-align:center;">Recent Work</h2>
  <div class="port-grid reveal">
    <article class="clip">
      <div class="clip-frame">
        <img class="clip-img" src="Captura de tela 2026-05-10 110501.png" alt="Clip 1">
      </div>
      <div class="clip-info">
        <div><div class="clip-title">Cards and Coffee</div><div class="clip-streamer">@lifestyle_clips</div></div>
      </div>
    </article>
    <article class="clip">
      <div class="clip-frame">
        <img class="clip-img" src="Captura de tela 2026-05-10 110559.png" alt="Clip 2">
      </div>
      <div class="clip-info">
        <div><div class="clip-title">DJ Moments</div><div class="clip-streamer">@stream_moments</div></div>
      </div>
    </article>
    <article class="clip">
      <div class="clip-frame">
        <img class="clip-img" src="Captura de tela 2026-05-10 181004.png" alt="Clip 3">
      </div>
      <div class="clip-info">
        <div><div class="clip-title">Mountain Adventure</div><div class="clip-streamer">@travel_vibe</div></div>
      </div>
    </article>
  </div>
</section>

<section class="pricing-bg" id="pricing">
  <div class="reveal">
    <h2>Simple Pricing</h2>
    <div class="pricing-grid">
      <div class="card">
        <h3>Starter</h3>
        <div class="price">$99<span>/one-time</span></div>
        <p>12 High-quality clips</p>
        <a href="https://arguellos.gumroad.com/l/streamlogics-starter" class="c-btn">Buy Starter</a>
      </div>
      <div class="card featured">
        <h3>Pro</h3>
        <div class="price">$119<span>/one-time</span></div>
        <p>20 Viral-ready clips</p>
        <a href="https://arguellos.gumroad.com/l/streamlogics-pro" class="c-btn">Buy Pro</a>
      </div>
      <div class="card">
        <h3>Premium</h3>
        <div class="price">$209<span>/month</span></div>
        <p>40 Clips + Full Management</p>
        <a href="https://arguellos.gumroad.com/l/StreamlogicsPremium" class="c-btn">Buy Premium</a>
      </div>
    </div>
  </div>
</section>

<script>
const obs = new IntersectionObserver(entries => {
  entries.forEach(e => { if(e.isIntersecting) e.target.classList.add('visible'); });
}, { threshold:0.1 });
document.querySelectorAll('.reveal').forEach(el => obs.observe(el));
</script>

</body>
</html>
